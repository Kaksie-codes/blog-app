

const Nodata = ({message}: {message:string}) => {
  return (
    <div className="w-full rounded-full p-4 bg-grey/50 mt-4 text-center ">
        <p>{message}</p>
    </div>
  )
}

export default Nodata